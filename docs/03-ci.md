# 3. CI

**Status :** ✅ fait — 4 jobs (lint, tests, build, docker_smoke) ; Jest + Testing Library en place.

---

## But

Valider le code (lint + tests unitaires et intégration), build l’app et l’image Docker, puis vérifier que le conteneur répond (smoke). Garantit que chaque push/PR est déployable.

---

## Concepts clés

- **Lint** : `npm run lint` (Next.js ESLint), job dédié.
- **Tests** : Jest + React Testing Library ; `npm test` (local), `npm run test:ci` (CI + coverage).
- **Build** : `npm run build` (mode standalone), après lint + tests.
- **Docker smoke** : build image, run container, GET :3000 → 200.

---

## Fichiers

| Fichier | Rôle |
|---------|------|
| `.github/workflows/ci.yml` | 4 jobs : **lint** → **tests** (parallèles), **build** (needs: lint + tests), **docker_smoke** (needs: build). |
| `jest.config.ts` | Config Next (next/jest), jsdom, setupFilesAfterEnv, alias `@/`. |
| `jest.setup.ts` | Import `@testing-library/jest-dom`. |
| `src/lib/__tests__/utils.test.ts` | Tests unitaires : `cn`, `slugify`. |
| `src/components/ui/__tests__/badge.test.tsx` | Tests UI : Badge (render, variant). |
| `src/components/ui/__tests__/button.test.tsx` | Tests interactifs : Button (click, disabled). |
| `src/components/main/__tests__/Footer.integration.test.tsx` | Test intégration : Footer (liens GitHub/LinkedIn, copyright). |

---

## Structure du workflow

| Job | Étapes |
|-----|--------|
| **lint** | Checkout → Setup Node → `npm ci` → `npm run lint` → Job summary |
| **tests** | Checkout → Setup Node → `npm ci` → `npm run test:ci` → Job summary |
| **build** | (needs: lint, tests) Checkout → Setup Node → `npm ci` → `npm run build` (DOCKER_BUILD=1) → Job summary |
| **docker_smoke** | (needs: build) Checkout → `docker build` → run container → curl 200 :3000 → stop/rm → Job summary |

---

## Scripts package.json

- `npm test` — Jest en mode watch (local).
- `npm run test:ci` — Jest en CI (single run, coverage, forceExit).

---

## Checklist

- [x] Jobs séparés : lint, tests, build, docker_smoke.
- [x] build dépend de lint + tests (parallèles).
- [x] Tests unitaires : utils (cn, slugify), Badge, Button (click/disabled).
- [x] Test intégration : Footer (contenu + liens).
- [x] Pas de snapshots ; tests ciblés et rapides.
- [x] Job summary par job.

---

## Troubleshooting

| Problème | Piste |
|----------|--------|
| Workflow ne se déclenche pas | Vérifier `on: push/pull_request` et la branche `main`. |
| Tests timeout / open handles | `test:ci` utilise `--forceExit` ; en local, vérifier mocks (next-themes, etc.). |
| Docker build échoue (workerd) | Voir [02-docker.md](02-docker.md) : DOCKER_BUILD=1. |
| Smoke timeout | Délai 10 s avant curl ; augmenter si besoin. |
